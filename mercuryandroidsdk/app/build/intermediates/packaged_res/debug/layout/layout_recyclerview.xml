<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/rayneo_safety_padding_horizontal"
    android:paddingVertical="@dimen/rayneo_safety_padding_vertical"
    android:visibility="visible">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="16dp"
        android:gravity="center"
        android:text="FixFocusPos"
        android:textColor="@color/white"
        android:textSize="26sp"
        android:textStyle="bold" />


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        style="@style/RecycleViewScrollBarStyle"
        android:layout_width="match_parent"
        android:layout_below="@+id/tv_title"
        tools:listitem="@layout/item_telephone_favorite" />

    <ImageView
        android:id="@+id/iv_select_hover"
        android:layout_width="match_parent"
        android:layout_height="118dp"
        android:layout_alignTop="@+id/recycler_view"
        android:layout_marginStart="69dp"
        android:layout_marginTop="-18dp"
        android:layout_marginEnd="67dp"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingEnd="20dp"
        android:scaleType="fitXY"
        android:src="@drawable/ic_tele_list_hover"
        android:visibility="gone"
        tools:visibility="visible" />

</RelativeLayout>